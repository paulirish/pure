function _pure_prompt_ending
    #echo "$pure_color_normal "
    echo "$pure_color_normal "  # this whitespace character is an EM QUAD. this way i dont lose space-prefixed commands to history/histcontrol
end
